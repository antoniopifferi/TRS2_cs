<UserControl x:Class="TRS2.StringBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:TRS2"
             MinHeight="28">
    <Grid Margin="0,0,0,10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80" />
            <ColumnDefinition Width="90" />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0"
                   Margin="0,0,8,0"
                   VerticalAlignment="Center"
                   Text="{Binding Label, RelativeSource={RelativeSource AncestorType={x:Type local:StringBox}}}" />
        <TextBox Grid.Column="1"
                 VerticalAlignment="Center"
                 Text="{Binding Value, RelativeSource={RelativeSource AncestorType={x:Type local:StringBox}}, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
    </Grid>
</UserControl>
